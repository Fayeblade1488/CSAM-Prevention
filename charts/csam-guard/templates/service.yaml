apiVersion: v1
kind: Service
metadata:
  name: {{ include "csam-guard.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "csam-guard.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8000
